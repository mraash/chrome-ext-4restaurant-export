import { CellKeyType, CellValueType } from './types/excel';

export class SheetData {
    private worksheet: Array<Array<any>> = [];

    public setCell(key: CellKeyType, value: CellValueType): void {
    }
}
